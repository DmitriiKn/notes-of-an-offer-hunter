Проектирование отказоустойчивых систем в микросервисной архитектуре, особенно в таком критичном домене, как банковский, требует использования специфических паттернов для контроля над взаимодействием сервисов. Основные из них — **Circuit Breaker** (предохранитель), **Retry** (повтор) и **Bulkhead** (разделение). Эти паттерны помогают изолировать сбои, управлять нагрузкой и предотвращать каскадные отказы.

Библиотека **Resilience4j** стала стандартом для Java-разработчиков в Spring-экосистеме, предоставляя удобные способы реализации этих паттернов.

### 🔧 Основные паттерны устойчивости и их реализация

**Circuit Breaker (Предохранитель)**

*   **Назначение** и **Механизм**: Защищает от постоянных вызовов падающего сервиса. После превышения порога ошибок "разрывает цепь" (переходит в состояние `OPEN`), блокируя вызовы и давая сервису время на восстановление. Через заданное время переходит в состояние `HALF_OPEN` для тестовых запросов и, если они успешны, возвращается в `CLOSED`.
*   **Ключевые параметры** (Resilience4j):
    *   `failureRateThreshold`: Процент ошибок для перехода в `OPEN` (по умолчанию 50%).
    *   `waitDurationInOpenState`: Время в состоянии `OPEN` перед переходом в `HALF_OPEN`.
    *   `permittedNumberOfCallsInHalfOpenState`: Количество тестовых вызовов в `HALF_OPEN`.
    *   `slidingWindowSize`: Количество вызовов для расчёта ошибок.
*   **Реализация в Spring Boot**:
    ```java
    @Service
    public class PaymentService {
        @CircuitBreaker(name = "paymentService", fallbackMethod = "processPaymentFallback")
        public String processPayment(PaymentRequest request) {
            // Вызов внешнего платёжного шлюза
            return externalGateway.call(request);
        }
        private String processPaymentFallback(PaymentRequest request, Exception e) {
            // Логика отката: вернуть клиенту понятную ошибку, записать в очередь на повтор и т.д.
            return "Платёжная система временно недоступна. Попробуйте позже.";
        }
    }
    ```
*   **Конфигурация в `application.yml`**:
    ```yaml
    resilience4j.circuitbreaker:
      instances:
        paymentService:
          slidingWindowSize: 10
          failureRateThreshold: 50
          waitDurationInOpenState: 60s
          permittedNumberOfCallsInHalfOpenState: 3
    ```

**Retry (Повтор)**

*   **Назначение**: Автоматически повторяет неудачный вызов для обработки временных сбоев (например, сетевые проблемы).
*   **Ключевые параметры**:
    *   `maxAttempts`: Общее число попыток (первая + ретраи).
    *   `waitDuration`: Пауза между попытками. Поддерживается экспоненциальный бэк-офф.
*   **Важное правило**: Применяйте **только к идемпотентным операциям** (например, запрос баланса), чтобы избежать побочных эффектов (например, двойного списания).
*   **Реализация**:
    ```java
    @Service
    public class ScoringService {
        @Retry(name = "scoringRetry", fallbackMethod = "getScoreFallback")
        public ScoringResult getScore(ClientData data) {
            // Вызов удалённого сервиса скоринга
            return remoteScoringClient.evaluate(data);
        }
        public ScoringResult getScoreFallback(ClientData data, Exception e) {
            return ScoringResult.defaultDecision(); // Консервативное решение по умолчанию
        }
    }
    ```

**Bulkhead (Разделение)**

*   **Назначение**: Изолирует ресурсы (потоки, соединения) для разных частей системы, чтобы сбой или перегрузка одного компонента не затронула все остальные.
*   **Типы в Resilience4j**:
    *   **SemaphoreBulkhead**: Ограничивает количество одновременных вызовов к методу.
    *   **ThreadPoolBulkhead**: Выделяет отдельный пул потоков для выполнения задачи.
*   **Ключевые параметры** (для `SemaphoreBulkhead`):
    *   `maxConcurrentCalls`: Максимум одновременных вызовов.
    *   `maxWaitDuration`: Время ожидания разрешения, если лимит исчерпан.
*   **Реализация**:
    ```java
    @Service
    public class ReportService {
        // Этот "тяжёлый" отчёт не должен блокировать основные платежи
        @Bulkhead(name = "reportBulkhead", type = Bulkhead.Type.SEMAPHORE, fallbackMethod = "generateReportFallback")
        public Report generateComplexReport(ReportRequest request) {
            // Долгое формирование отчёта
            return heavyReportGenerator.build(request);
        }
    }
    ```

### 🗺️ Как правильно комбинировать паттерны

Эти паттерны редко используются по отдельности. Вот как их правильно сочетать:

**1. Объединение Circuit Breaker и Retry**
*   **Принцип**: Ретраи должны выполняться **только пока Circuit Breaker находится в состоянии `CLOSED`**. Если предохранитель сработал (`OPEN`), немедленный отказ предотвращает лишнюю нагрузку.
*   **Порядок**: С точки зрения кода, `Retry` должен быть "внутри" `CircuitBreaker`. В Resilience4j такой порядок аспектов применяется по умолчанию.

**2. Объединение Circuit Breaker и Bulkhead**
*   **Принцип**: `Bulkhead` изолирует выполнение метода в отдельном пуле ресурсов, а `CircuitBreaker` следит за ошибками в этом пуле. Это защищает систему на двух уровнях.
*   **Порядок**: `CircuitBreaker` обычно оборачивает `Bulkhead`.

### 📊 Настройка и мониторинг в продакшене

**Конфигурация через свойства Spring Boot**
Resilience4j позволяет гибко настраивать инстансы через `application.yml`, задавая как общие конфигурации (`configs`), так и параметры для конкретных инстансов (`instances`).

**Мониторинг через Spring Boot Actuator**
Подключив `spring-boot-starter-actuator`, вы получаете готовые эндпоинты для наблюдения:
*   `/actuator/health`: Отображает статус Circuit Breaker'ов.
*   `/actuator/circuitbreakers`: Детальная информация о состоянии всех предохранителей.
*   `/actuator/metrics/resilience4j.circuitbreaker.calls`: Метрики вызовов.
*   `/actuator/prometheus`: Экспорт метрик в формате для Prometheus.

**Настройка зависимостей (Maven)**
Для начала работы добавьте в `pom.xml`:
```xml
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-aop</artifactId> <!-- Для аннотаций -->
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId> <!-- Для мониторинга -->
</dependency>
```

### 💎 Ключевые выводы для банковской разработки

1.  **Fail Fast (Быстрый отказ)**: Используйте **Circuit Breaker**, чтобы не пытаться безнадёжно вызвать упавший сервис.
2.  **Изоляция (Isolation)**: Применяйте **Bulkhead** для критически важных (например, платёжных) и ресурсоёмких (например, генерация отчётов) процессов.
3.  **Умный Retry (Smart Retry)**: Используйте **Retry** с бэк-оффом только для идемпотентных операций и в связке с Circuit Breaker.
4.  **Graceful Degradation (Грациозная деградация)**: Всегда реализуйте понятные **Fallback**-механизмы для пользователей.

Настройка параметров (количество ретраев, порог срабатывания) — это не теоретическая задача, а практический компромисс, который необходимо находить, наблюдая за поведением системы в продлении. Стартовые значения из примеров — хорошая точка отсчёта для экспериментов.
